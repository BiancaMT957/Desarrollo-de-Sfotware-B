{
  "variable": {
    "network": {
      "type": "string",
      "default": "net2-peered",
      "description": "Red usada por env3. Depende de net2 (peering simulado)"
    },
    "name": {
      "type": "string",
      "default": "env3",
      "description": "Nombre de la instancia"
    },
    "port": {
      "type": "number",
      "default": 8082,
      "description": "Puerto de la app"
    },
    "api_key": {
      "type": "string",
      "default": "",
      "description": "API key (leer desde entorno / secure.json)"
    }
  }
}